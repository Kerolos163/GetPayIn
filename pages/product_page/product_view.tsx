import React from "react";
import { View, Text } from "react-native";

const ProductView = () => {
  return <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text>ProductView Pag</Text>
  </View>;
};

export default ProductView;
