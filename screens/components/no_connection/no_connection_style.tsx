import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  body: {
    flex: 1,
    backgroundColor: "white",
    paddingTop: 40,
    paddingHorizontal: 4,
  },
  container: { width: "100%", height: "100%", resizeMode: "contain" },
});

export default styles;
